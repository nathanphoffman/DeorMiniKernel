[package]
name = "DeorMiniKernel"
version = "0.1.0"
edition = "2021"

[lib]
name = "deor_mini_kernel"
crate-type = ["staticlib"]
path = "src/lib.rs"

[profile.dev]
panic = "abort"

[profile.release]
panic = "abort"
